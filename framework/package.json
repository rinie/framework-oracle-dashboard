{
  "name": "oracle-dashboard-framework",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd .. && observable preview --root framework",
    "build": "cd .. && observable build --root framework",
    "preview": "cd .. && observable preview --no-open --root framework"
  },
  "dependencies": {
    "@observablehq/framework": "^1.9.0",
    "apache-arrow": "^14.0.2",
    "duckdb": "^1.4.4"
  }
}
